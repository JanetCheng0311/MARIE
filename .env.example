# Copy this file to .env and fill in your credentials.
# 將此檔複製為 .env 並填入您的金鑰。

# Optional server endpoint (overrides hardcoded default)
SERVER_IP=http://YOUR_SERVER_IP:PORT/api/v0/chat/completions

# API key placeholder (if you later use an api key in the code)
MARIE_API_KEY=your_marie_api_key_here
#Minimax API key placeholder
MINIMAX_API_KEY=your_minimax_api_key_here

# Minimax API base URL (optional). Examples:
# https://api.minimaxi.chat/v1  or  https://api.minimax.io/v1
MINIMAX_API_URL=https://api.minimaxi.chat/v1

# Optional: pre-list voice IDs for quick selection (comma-separated)
#MINIMAX_VOICE_IDS=ClonedVoice20260102142340,ClonedVoice20260102164254

# Clone options (optional)
# model used for cloning
MINIMAX_CLONE_MODEL=speech-2.6-hd
# whether to apply noise reduction to uploaded samples
MINIMAX_CLONE_NOISE_REDUCTION=true
# whether to normalize volume
MINIMAX_CLONE_NORMALIZE=true
# short preview text included when creating the clone
MINIMAX_CLONE_PREVIEW_TEXT=測試語音輸出

# Selection limits for sample packing
# total seconds of samples allowed (default 300)
MAX_VOICE_SAMPLE_SECONDS=300
# maximum number of files to include when creating a clone (default 7)
MAX_VOICE_SAMPLE_COUNT=7

# Example voice IDs (do not commit your real voice IDs)
VoiceID01=ClonedVoice20260102174103
VoiceID02=your_voice_id_02_here